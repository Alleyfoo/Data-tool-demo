Working Version Note

This repository currently contains a working Streamlit UI with batch Excel processing.

Highlights:
- Upload + metadata selection builds a header-aware preview dataframe.
- Mapping + Query Builder reuse the same preview cache.
- Template Library supports batch processing and combine outputs.
